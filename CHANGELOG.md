# Suspect - Changelog

Maintained per-prompt as a durable development record; entries are appended chronologically so the file survives context loss.

## Project overview

Suspect is a Roblox social-deduction game (crewmates vs. impostors) built as a Rojo project. The project has since pivoted to the "Séance/Banishment" design — a candlelit manor built on this same skeleton — with `docs/DESIGN.md` as the authoritative design contract that supersedes the old roadmap's gameplay plans. What exists in the code today:

- **Round loop** - `MatchService` waits for a minimum player count, runs an intermission countdown, plays the match, shows an end screen, and loops. Late joiners spectate and spawn into the next round.
- **Roles, kills, meetings** - `RoleManager` is the single source of truth for role and alive-state; `KillSystem` does proximity/cooldown-validated kills with ragdoll bodies; `MeetingSystem` runs body reports and emergency meetings, seats players at a table, tallies votes and ejects. Dead players get a spectate camera with target cycling.
- **Powerup economy** - five powerups (SpeedBoost, Flashlight, Invisibility, Shapeshifter, Seer) across two independent axes: rarity (fixed, drives gacha odds) and tier 1-3 (personal upgrade track). `GachaService` rolls which powerup you get by weight with a pity counter; duplicates bank toward tier upgrades; `LoadoutService` stages a pending 2-slot loadout that only becomes active at match start.
- **Task system** - `TaskManager` assigns a per-match profile of short/long tasks from stations tagged in the place file, validated server-side by session, proximity and minimum duration. Ten minigames are implemented (WireSplice, DialMatch, HoldFill, SliderSync, PrecisionPins, SortStow, EchoCode, ScrubDown, SpotCheck, FlowRoute) plus a Placeholder fallback.
- **Sabotage** - `SabotageService` owns impostor-triggered sabotages (Lights, and a critical Boiler on a countdown that can end the match), cooldowns, and fix sessions routed through the same client task pipeline (FixSwitches, FixValve).
- **UI** - a shared `UIStyle` module backs a persistent bottom bar, a tabbed hub window (Store/Inventory), the task list, powerup HUD, round-status and sabotage banners, meeting UI, end screen and spectate bar.

## History

### 2026-07-15 - Project scaffold and first services
- Initial commit: Rojo project scaffold plus the core server services.
- `TaskManager` and the tagged task-station handler added and wired into `Bootstrap`.

### 2026-07-16 to 2026-07-20 - Kills, meetings, ragdolls, test map
- Split powerup ownership from the equipped loadout into a 2-slot system; added the missing `SetLoadout`/`LoadoutResult` remote names.
- `KillSystem`: proximity- and cooldown-validated kills with a ragdoll on death.
- `MeetingSystem`: body reports, emergency meetings, voting UI and ejection (shipped untested).
- Debug mode to make all players impostors (and let impostors kill each other) for small-group testing.
- Several ragdoll iterations, ending on the `BreakJoints` approach with collision so bodies can be pushed, and a fix for flailing by zeroing Humanoid health after joints are replaced with constraints.
- Round-end cleanup: result banner persists per round, bodies cleared at round end.
- Added the test map "The Estate" to the place file, with a note that the UI needed a full redo before tasks were fleshed out.

### 2026-07-22 - `[major]` Core gameplay fixes + crew task list UI
- Task stations no longer disabled globally after one player completed them (this had been silently blocking the crew task win condition).
- Tasks assigned only to crew, after roles are decided; match start became a one-shot gate with a join grace period so roles/tasks stopped resetting on every join.
- New `TasksUpdated` remote and `TaskListUI` showing each crew member their own tasks with live done/total progress.
- Meeting safety: kills and powerup use blocked during meetings, votes validated against the meeting's alive snapshot, active speed boosts canceled at meeting start via the new `OnMeetingStart` hook.

### 2026-07-22 - `[major]` Match end flow, end screen, spectate + ghost gacha
- New `MatchService`: win condition evaluated on kills, meeting resolutions **and** task completion (which previously could never end a match), with parity excluded from task-triggered checks so finishing a task can't hand impostors a win.
- `MatchEnded` broadcast, a token-guarded `EndScreenUI`, and an in-place restart standing in for the future lobby teleport; the `OnMatchStart` hook lets services reset their own state.
- New `SpectateService`: dead players follow the living with Q/E cycling. The alive list is broadcast to dead players only, so living clients can't detect unreported kills.
- Gacha usable while dead, plus a new `GetGachaCatalog` RemoteFunction; `GrantOrUpgrade` now returns New/Upgraded/Duplicate instead of a boolean, fixing every roll being mislabeled a duplicate.
- All-impostor debug consolidated into a single `DebugFlags` flag with a loud server-start warning.

### 2026-07-22 - `[major]` Powerup rework (shipped untested, verified later)
- Replaced the Common/Rare/Epic *variant* system: rarity became a fixed property driving gacha odds, and tiers 1-3 became the personal upgrade track - two separate axes.
- Ownership is tier + duplicates; rolling never auto-upgrades. Three duplicates buy one tier, max Tier 3, via a server-side `TryUpgrade`.
- Gacha rolls which powerup by weight (Common 60% / Rare 20% / Epic 20%); pity guarantees Rare-or-better within 10 rolls.
- Deleted Decoy, VisionPulse and VentLock; shipped the five launch powerups with per-tier stats.
- Loadout system with pending vs. active sets, so a save stages the next match and the active set is locked for the current one.
- Generic active-effect registry with identity-token timers; new `OnKillPerformed` and `OnLightsChanged` hooks; a minimal `LightsSystem` stub so Flashlight was testable.
- Rough UI added: `PowerupHUD`, `LoadoutUI`, reworked gacha panel, and `PowerupFX` as the single owner of client-side Lighting.

### 2026-07-23 - `[major]` Round loop, task framework, first minigame
- Replaced the one-shot match-start gate with a real round loop, fixing the published-game bug where the first joiner started a solo match and later arrivals were left roleless.
- Late joiners became full spectators with gacha and loadout access; new `RoundStatus` remote and banner; leaving mid-match triggers a win check; empty-server wedge guard.
- Task framework: stations open a real task window, with server-tracked sessions validated by proximity and minimum duration. Assignment became profile-based (7 tasks split short/long, never repeating the previous match's split), backed by a new shared `TaskDefs` module.
- **Wire Splice**, the first real minigame, on the contract every later minigame follows.
- Emergency meetings moved from a global M keybind to a physical tagged button at the round table.
- Confirmed the previous commit's powerup rework as playtested end to end.

### 2026-07-23 - `[major]` Echo Code + task testing flag
- **Echo Code**, the first long minigame: a Simon-style flashing sequence over three extending rounds, with infinite retries so the cost of failure is time.
- Audio deliberately scoped to this one task, pitched from a single engine-bundled `rbxasset` sound so there's no marketplace or moderation dependency.
- New `ASSIGN_ALL_TASKS` debug flag giving every crew player every registered task, replacing manual profile-table edits.

### 2026-07-24 - `[major]` MILESTONE: full 10-task minigame pool + E-only world prompts
- All ten generic minigames playable, completing the task pool phase: the five remaining shorts (Dial Match, Hold and Fill, Precision Pins, Slider Sync, Sort and Stow) and three longs (Scrub Down, Spot Check, Flow Route, the last generated backwards from a carved solution so it's solvable by construction).
- Input philosophy locked in: F for timing tasks with on-screen fallbacks, drag where physical, click where spatial.
- Solved the "tasks reset on click" mystery: ProximityPrompts are mouse-clickable by default and GUI frames pass clicks through, so clicking inside a task window re-triggered the station behind it. All world prompts are now E-only, task windows sink clicks, and a new `TaskCancel` remote clears sessions so stations can't wedge.
- Known mobile debt recorded: touch devices activate prompts by tapping, so mobile can't start world interactions until a per-platform input pass.

### 2026-07-24 - Window drag smoothness + powerup badge readability
- `UIStyle.MakeDraggable` rewritten to move by the delta from the grab point added to the panel's position at the grab, rather than re-deriving position from the absolute pointer each frame - the window now tracks the cursor 1:1 instead of snapping on the first frame of a drag.
- Grab state renamed to `dragStartPointer`/`dragStartPos` and captured on press; the 40px on-screen clamp still applies to the final position, and the resize/drag mutual-exclusion guard (no drag starts within `EDGE` px of a border or while a resize is active) is unchanged.
- Powerup HUD keybind badges enlarged from 16x14 to a 20x20 key cap: dark `Bg` chip with a 1px Accent stroke, 3px padding, rounded corner, and the digit in bold Accent `TextScaled` so it is centered and no longer smashed into the slot corner. The slot's name label, cooldown overlay, countdown, status line and Seer toast are untouched.

### 2026-07-24 - Hub window locked to a fixed size
- Removed `MakeResizable` from the hub window entirely; the resize input layer was interfering with it and shifting it around. No resize grip, handle or bounds references remain in `HubUI`.
- The hub is now a fixed `540x480`, anchor `(0.5, 0.5)`, position `UDim2.new(0.5, 0, 0.5, 0)`, stored as two constants and applied on open. The session position/size memory (the saved geometry and its two `GetPropertyChangedSignal` listeners) is gone, so nothing re-applies a remembered size.
- Tab switching touches only the content frame: `openTab` clears and rebuilds the inner content and re-tints the tab buttons, and never reads or writes the window's Position or Size.
- The header stays draggable - `MakeDraggable` only moves the panel from an `InputBegan` on the header strip followed by pointer movement, so opening the hub or switching tabs cannot move it.
- `UIStyle.MakeResizable` is unchanged and still in use by the task list, which keeps its edge/corner resizing.

### 2026-07-24 - Custom pixel keybind prompts

- Every world ProximityPrompt is switched to `ProximityPromptStyle.Custom` server-side, so the default Roblox prompt UI no longer appears anywhere: task stations, the emergency button, sabotage fix stations and reported bodies.
- Prompt labels corrected at the source: task stations now use the task's themed `TaskDefs` displayName as `ActionText` (with the Generic fallback reading "Use" instead of "Do the Task"), the emergency button reads "Call meeting", body reports read "Report body" (set on every setup, not only when the prompt is created), and sabotage stations keep "Fix". No other prompt config (`ClickablePrompt`, `HoldDuration`, enable/disable logic) changed.
- New client `PromptUI.client.lua` renders the replacement: driven entirely off `ProximityPromptService` (`PromptShown`/`PromptHidden`), it builds one `BillboardGui` per prompt adorned to the interactable - a pixel `PressStart2P` action label with a solid-black outline over a chunky keycap (dark face, near-white 3px stroke, offset drop-shadow, the keyboard key letter in Accent). Hard edges only, no `UICorner`.
- Hold prompts animate a bottom-to-full Accent fill inside the keycap over `HoldDuration` via `PromptButtonHoldBegan`/`Ended`; zero-duration prompts never show a fill. Empty `ActionText` hides the label row and centers the keycap alone.
- Known debt recorded in-file: the keycap always shows the keyboard keycode (desktop-first); the per-platform mobile input pass replaces it later. `PressStart2P` is engine-bundled, with the one font string flagged as the single swap point if it ever fails to resolve.

### 2026-07-24 - Meeting & voting UI restyled against UIStyle

- Presentation-only cleanup of `MeetingUI.client.lua`; every piece of logic, timing and remote handling (`MeetingStarted`/`VoteResult`/`CastVote`, the `hasVoted` guard, the rebuild-each-meeting flow, the result banner persisting for the whole following round) is unchanged. Not the final meeting redesign.
- Added a full-screen dim overlay (black, `BackgroundTransparency 0.45`) behind the meeting panel. The voting frame became a transparent full-screen container so its existing `Visible` toggle shows/hides the overlay and panel together.
- The meeting panel is now a centered `UIStyle.MakePanel` (520 wide) with a `MakeHeader` carrying the same report-vs-emergency title the code already sets; voter rows are `MakeButton`s (player name left, TextPrimary) in a scroller, and Skip is its own `MakeButton` in a holder visually separated below the list.
- The result banner became a centered `MakePanel` with `HeaderFont` text tinted Positive for "No one was ejected." and Negative for an ejection, same display duration and clear-on-next-meeting behavior.
- Montserrat font faces and standard text strokes throughout. No countdown bar was added: the current UI has no timer (`_duration` is received but unused), and no per-target vote-selection styling was added because no such selection state exists in the code - neither feature was invented.

### 2026-07-24 - Meeting countdown bar + vote-selection tint

- Two additions the restyle pass deliberately excluded, now authorized, in `MeetingUI.client.lua` only. Verified first (read-only) that `MeetingSystem` genuinely enforces the window: it sends `MEETING_DURATION` on meeting start and `task.delay(MEETING_DURATION, ResolveMeeting)` auto-resolves on the same value, so the bar visualizes a real timeout and can safely reach zero. `MeetingSystem` was not modified.
- Added a thin 4px countdown bar directly under the panel header: Row-colored track, Accent fill starting full and depleting linearly to zero over the server-sent duration. Heartbeat-driven and token-guarded (a new meeting or the meeting ending invalidates the running loop), shown on meeting start and hidden by the same `VoteResult` flow that ends the meeting display. Purely cosmetic - the client takes no action when the bar empties; the server owns resolution.
- The meeting-start handler now uses its previously underscore-ignored duration parameter to drive the bar. A missing/non-positive duration leaves the bar full rather than counting down to nothing.
- Vote-selection tint: casting a vote (a player row or Skip) now marks that row via `UIStyle.SetButtonSelected(row, true, Accent)`; the single `hasVoted` guard means no other row is ever selected. The tint is cleared when the meeting display ends and the reference is dropped when rows are rebuilt. This mirrors the client-side assumption the `hasVoted` guard already makes (the cast vote is treated as landed); no server vote-confirmation exists and none was invented.

### 2026-07-24 - Kill feedback (impact, mark, sound, cooldown chip)

- New `KillFeedback` server->client remote. On a successful kill `KillSystem` fires it to the killer with the kill cooldown seconds, and adds two pieces of world feedback at the body.
- World mark & sound: a positional `snap.mp3` (engine-bundled, PlaybackSpeed 0.85, Volume 0.6, Inverse rolloff to 35 studs) hosted on the corpse torso/root - audible within ~35 studs by design, kills are information - and a thin dark-red blood-pool Cylinder (~4 studs, 0.1 tall, anchored, no-collide/no-query) raycast onto the floor under the corpse and parented to the corpse model so every existing body-cleanup path removes it automatically. No decals or particles.
- New client `KillFX.client.lua`, layered on top of the existing flow without touching it: the KILLER gets a quick red full-screen vignette (0.55->1 over 0.35s) plus a +4 FOV punch tweened back over 0.25s (no shake); the VICTIM gets a brief red flash (0.4->1 over 0.5s) on their own `PlayerDied` before the existing death/spectate flow; and impostors get a pixel "F" keycap chip (PromptUI-style) to the left of the powerup slots that, on a kill, runs a darkening sweep + countdown over the cooldown then clears.
- The chip is impostor-only via `RoleAssigned`, hidden for crew, and every transient effect is guarded (tween-cancel or a Heartbeat token) and reset on `CharacterAdded`/`MatchEnded`.
- Accepted as-is: no character kill animation (ragdoll is the animation), the snap sound is a placeholder for the audio pass, and the chip only shows a countdown after the first kill (no match-start readiness signal yet).

### 2026-07-24 - Settings system core (module + window)

- New `ClientSettings.lua` ModuleScript: the single source of truth for client settings, session-only by design (DataStore persistence is a later phase, noted in the header). Holds keybind defaults + display names for nine remappable actions (Kill/Store/Inventory/Sabotage/Powerup1/Powerup2/SpectatePrev/SpectateNext/TaskAction), a `RESERVED` set (W/A/S/D/Space/E - movement plus the shared server-side prompt interact key), and the API: `GetKey`/`SetKey` (rejects `Reserved` and `InUse`, duplicate detection across remappable actions only), `GetVolume`/`SetVolume`, `GetReduceEffects`/`SetReduceEffects`, `ApplyVolume(base)`, a `Changed` BindableEvent fired with the setting name, and a `ResetLayout` BindableEvent with `FireResetLayout()`.
- New `SettingsUI.client.lua`: a bottom-left cog button (UIStyle square, emoji with a documented "Set" text fallback) toggles a fixed-size (380x460) draggable `MakePanel` window with a `MakeHeader` "Settings" + X, opened centered-left and force-hidden on meeting start like the hub.
- Window sections: Audio (a delta-drag master-volume slider with Accent handle/fill and a percent label, reading/writing `ClientSettings` volume); Keybinds (one row per action in a scroller with a PromptUI-style pixel keycap chip, a click-to-capture remap flow - "Press a key...", Escape cancels, Reserved/InUse flash a red notice and keep the old binding, one capture at a time - plus a fixed dimmed "Interact (world) [E]" row and a reserved-key note); Interface (a "Reset UI layout" button calling `FireResetLayout`); Accessibility (a "Reduce screen effects" ON/OFF toggle, Positive tint when on).
- Rows repaint live from `ClientSettings.Changed`. Nothing consumes these values yet - the next prompt migrates every consumer; the cog is text/emoji until the art pass.

### 2026-07-24 - Settings wired into every consumer

- Every hardcoded keybind now resolves through `ClientSettings.GetKey(...)` compared at input time inside the existing `InputBegan`/`InputEnded` handlers (never cached at startup), so remaps apply instantly with no reconnection; every existing `gameProcessed` guard is preserved. Migrated: `KillInput` (Kill), `HubUI` (Store/Inventory), `SabotagePanel` (Sabotage), `PowerupHUD` (Powerup1/2), `SpectateUI` (SpectatePrev/Next), and the `DialMatch`/`HoldFill`/`PrecisionPins` task hotkeys (TaskAction). The now-dead `KILL_KEY`/`PREV_KEY`/`NEXT_KEY`/`TOGGLE_KEY` constants were removed.
- On-screen key labels re-read the current binding: PowerupHUD slot badges (One->"1", Two->"2", letters as-is), the SpectateUI legend ("G - Store   L - Inventory"), BottomBar buttons ("Store [G]" / "Inventory [L]"), the KillFX cooldown chip keycap, and the `DialMatch`/`HoldFill` "HOLD [F]" buttons - all refresh on `ClientSettings.Changed` (task buttons render at Build time).
- Client-created sounds pass their final Volume through `ClientSettings.ApplyVolume(base)`: EchoCode's `playNote` and HubUI's gacha tick/landing/Epic notes. Each site notes that the master slider governs client sounds while world/positional sounds join via SoundGroups in the audio pass.
- Accessibility: KillFX skips the killer vignette and FOV punch entirely when `GetReduceEffects()` is true; the victim flash still plays (it communicates death, not flair) and the cooldown chip is informational, both commented.
- Layout reset: TaskListUI (position + size) and SabotagePanel (position) subscribe to `ClientSettings.ResetLayout` and restore their captured defaults, clearing their session-remembered values.
- Unchanged by design: PromptUI's world keycap still shows the prompt's own fixed E; the kill snap and server-side sounds ignore the slider until the audio pass; settings remain session-only.

### 2026-07-24 - Settings window playtest fixes

- Layout: the settings window is now a fixed 400x560 (was 380x460), opens centered-left, drags by its header, and has no resize of any kind. Everything below the header is now ONE `ScrollingFrame` (UIListLayout with 8px padding, `AutomaticCanvasSize` Y, `ScrollBarThickness` 4, 12px right padding so nothing sits under the scrollbar). The nested keybinds scroller was deleted - keybind rows flow directly in the main list between section labels, so every section (including the Reduce screen effects row) is fully reachable and never half-cut.
- Fonts: audited every text instance - all labels, headers, buttons, notices and hints use `UIStyle` Montserrat faces with the standard stroke; the pixel `PressStart2P` font now appears in exactly one place, the key text inside keycap chips.
- Keycap chips: each is a full square Frame (min 30x30, no `UICorner`, a complete four-sided 2px near-white `UIStroke`, dark fill), right-aligned with clearance from the scroll edge so the border is never clipped. Displayed key uses the same short-name mapping as the HUD badges (One-Nine -> "1".."9", Zero -> "0", single letters as-is, else `KeyCode.Name`), and the chip auto-widens up to ~64px for longer names instead of crushing the text. The fixed "Interact (world)" row keeps its dimmed styling with the same full-square chip.
- Capture feedback: clicking a chip switches its stroke to Accent and pulses it (transparency tweened 0<->0.6 on a loop, token-guarded), replaces the key text with a keyboard glyph, and adds a "(press a key)" suffix to the row label. Success stops the pulse and flashes the chip Positive before showing the new key; Reserved/InUse flash Negative alongside the row notice and keep the old key; Escape cancels cleanly. Starting a capture on a second chip cancels the first.

### 2026-07-25 - Design pivot: docs realigned to DESIGN.md

- The project pivoted from the old three-map / generic-task / rarity+tier plan to the "Séance/Banishment" design. `docs/DESIGN.md` (added manually) is now the authoritative design contract; the doc changes below align the repo's documentation to it. No code changed.
- Deleted the superseded `suspect-roblox-roadmap.md` at the repo root, which described the abandoned three-map scope, generic task reskins and the old powerup model.
- Added `docs/ROADMAP.md`: a one-page phase view condensed from DESIGN.md §11 - a two-line status header (skeleton complete; pivoting to Séance/Banishment) then Phase 0 through Phase 6 with each phase's goal and playable milestone, plus a deferred list (venues 2+, cosmetics content, mobile pass, charge system, doom clock) per DESIGN.md §12.
- Updated this file's Project overview to note the pivot and point to `docs/DESIGN.md`; History entries were left untouched.

### 2026-07-25 - Phase 0 prep (1/5): role & winner ids centralized

- New `ReplicatedStorage/Modules/GameConstants.lua` (no requires): `GameConstants.Roles` (`Crew = "Crewmate"`, `Vessel = "Impostor"`) and `GameConstants.Winners` (`Crew = "CrewWin"`, `Vessel = "ImpostorWin"`). Values are the exact existing literals, discovered from `RoleManager`/`MatchService` - byte-identical, nothing renamed. These are internal ids; the Séance re-theme changes display strings only, so the ids stay stable.
- Swept every comparison/assignment of those literals to reference the module instead. Changed: `RoleManager` (role assignment, `GetAllImpostors`/`GetAllCrew`, `CheckWinCondition` returns), `KillSystem` (killer/target role checks), `SabotageService` (`IMPOSTOR_ROLE` init + the boiler `ForceEnd` winner), and the clients `SabotagePanel`, `PowerupFX`, `KillFX` (each `IMPOSTOR_ROLE` init) and `EndScreenUI` (winner branch). Each now requires `GameConstants`.
- Checked and deliberately left alone: `MatchService` (relays the winner value from `RoleManager`, holds no literal of its own), `SpectateService`/`MeetingSystem` (role used via `GetRole` with no literal compare - MeetingSystem only passes `ejectedRole` through), `PowerupHUD` (displays the Seer role via `tostring`, no compare), `KillInput`, and all ten `TaskMinigames` (no role gating). The `"NotImpostor"`/`"CannotKillImpostor"` strings are error codes, not roles, and were untouched.
- Behavior is unchanged: this moves where the strings live, never their values. No per-service tuning was pulled into the module - only genuinely shared ids.

### 2026-07-25 - Phase 0 prep (2/5): crew objective behind a swappable provider

- `MatchService` now owns the crew's victory objective behind a seam: new `MatchService.SetCrewObjectiveProvider(fn)` (fn() -> boolean, true when the objective is complete). The default provider, set at the bottom of the file, is the verbatim task-completion computation that used to live inline in `EvaluateWinCondition` - `GetTotalCount() > 0 and GetRemainingCount() <= 0`, with `GetRemainingCount` still only read when tasks exist.
- `EvaluateWinCondition` no longer computes task progress itself: it calls `crewObjectiveProvider()` and passes the boolean onward. The `TaskCompleted` trigger, its parity exclusion (`includeParity = trigger ~= "TaskCompleted"`), and every other trigger are unchanged.
- A loud PHASE 1 SEAM comment marks the default provider: Phase 1's `RitualService` swaps in brazier/Convergence completion and removes the `TaskCompleted` hook then; until that day tasks win matches exactly as before.
- `RoleManager.CheckWinCondition`'s first parameter was renamed `tasksRemaining` -> `crewObjectiveComplete` and is now the boolean directly; the win clause became `elseif crewObjectiveComplete then` (was `tasksRemaining ~= nil and tasksRemaining <= 0`). Parity logic and every return value are unchanged, and the sole call site (MatchService) was updated.
- Byte-identical: no tasks -> false, remaining 0 -> true, remaining > 0 -> false, matching the old nil/`<= 0` behavior exactly.

### 2026-07-25 - Phase 0 prep (3/5): powerup data moved to shared CharmDefs

- New `ReplicatedStorage/Modules/CharmDefs.lua` (no requires): every powerup keyed by its existing id, carrying the live fields moved verbatim from `PowerupService` - `displayName`, `rarity`, `gachaWeight` (was `weight`), `cooldown`, and the per-tier `tiers` tables exactly as they were (including Seer's per-tier `usesPerMatch`, which is 1/1/2 and so stays inside the tiers, not hoisted). Same ids, names, odds, cooldowns and tier stats - nothing revalued.
- Added Phase 2 SCAFFOLD fields per docs/DESIGN.md sections 7 & 11, present but read by nothing yet: `loadoutWeight` (1/1/2/3/3), `essence` (Haste/Light/Veil/Mirage/Insight), `faces = { Crew, Vessel }`, `omen`. Header documents them as Phase 2 targets and names the module the single source of truth the Charm system interprets.
- `PowerupService` deleted its inline `Definitions` literal and now aliases `PowerupService.Definitions = CharmDefs.Charms`, so it interprets the shared data. Effect handlers, the active-effect registry, identity tokens, cooldown/use gates and every hook are untouched. `GetOdds` reads `def.gachaWeight`. The public alias keeps `Bootstrap`'s `DebugGrantMax` (iterates the ids) working with no change to that file.
- `GachaService` now requires `CharmDefs` and reads weights, rarity classes and catalog data from `CharmDefs.Charms` (`gachaWeight` for the weighted roll, `rarity` for pity, `displayName`/`rarity` for the catalog); pity logic is unchanged and the `GetCatalog` RemoteFunction returns the exact same shape clients depend on.
- No changes to `PowerupOwnershipService`, `LoadoutService`, `Bootstrap`, or any client file - they key by the stable ids.

### 2026-07-25 - Phase 0 prep (4/5): DataStore-backed player persistence

- New `ServerScriptService/Services/PlayerDataService.lua`: owns a DataStore ("SuspectPlayerData_v1") session per player. Schema `{ version = 1, currency = 500, ownership = { [charmId] = { tier, dupes } }, pity = 0, memoria = 0 }` (memoria is a Phase 3 scaffold - stored, never read yet). Loads on join with pcall + up to 3 retries and linear backoff; saves on `PlayerRemoving`, autosaves dirty sessions every 120s, and `game:BindToClose` flushes everyone with a Studio-shutdown wait. Public API is `Get`/`MarkDirty`/`IsLoaded`, plus an `OnLoaded` hook owning services use to hydrate. Promoted ahead of the Forge per docs/DESIGN.md sections 8 & 11.
- DATA-LOSS GUARD: when a load fails after every retry, the session is served DEFAULTS but marked UNSAVEABLE and warns loudly, so a failed load can never later overwrite real data in the store (autosave, leave-save and BindToClose all skip unsaveable sessions).
- Currency migrated onto persisted data: the `Currency` player Attribute is now a MIRROR set from `data.currency` on load; every spend goes through a small PlayerDataService-backed setter in `GachaService` that updates `data.currency`, the Attribute, and marks dirty. `Bootstrap` no longer hardcodes 500 on join - it sets the Attribute from data in an `OnLoaded` handler. Existing UI reading the Attribute is untouched.
- `PowerupOwnershipService` hydrates its in-memory `owned` cache from `data.ownership` on load (bridging persisted `dupes` <-> the cache's long-standing `duplicates`, so consumer signatures are unchanged) and writes each entry back + marks dirty on grant/dupe/upgrade. `DebugGrantMax` deliberately does not persist (session-only) and now runs post-load via `OnLoaded` so hydration can't clobber it. `Owns`/`GetOwned*`/`GrantOrDuplicate`/`TryUpgrade` signatures unchanged.
- `GachaService` pity now reads/writes `data.pity` (marking dirty), replacing the in-memory `pityCounter`; the `GetCatalog` RemoteFunction shape is unchanged and tolerates a not-yet-loaded session (pity reads 0).
- Join-order safety: `GachaService.Roll`, `LoadoutService.SetLoadout` and `PowerupOwnershipService.TryUpgrade` return a friendly `"StillLoading"` failure when called before `IsLoaded`, rather than acting on defaults; read-only catalog still populates during the load.
- No cycles: PlayerDataService requires only engine services; nothing requires it back.

### 2026-07-25 - Phase 0 prep (5/5): spirit + séance seams (skeleton only)

- New `ServerScriptService/Services/SpiritService.lua`: a dead-player registry. Players are added on `RoleManager.OnAliveChanged(alive == false)` and cleared on `MatchService.OnMatchStart` and `PlayerRemoving`. API is `IsSpirit(player)` and `GetSpirits()` (returns a fresh array). Cycle-safe: it requires RoleManager/MatchService (neither requires it back) and reacts only through their hooks. A loud comment block maps docs/DESIGN.md section 6 onto this seam - the Offer (allegiance) at registry-add, plus allegiance kits, Memoria, ghost chores and veil-shock reception all hanging here in Phase 3. Today it only tracks membership.
- `MeetingSystem` gained `RegisterSeancePhaseHook(cb)`; the registered callbacks are invoked once, right after the meeting is convened and announced (after `MeetingStarted` fires), marked with a loud PHASE 3 SEAM comment - the single point where the once-per-séance spirit question will run, noting it ships behind a flag (`DebugFlags.SPIRIT_QUESTION`). No hooks are registered today, so the loop is a no-op; zero change to meeting behavior or timing.
- `Bootstrap` requires `SpiritService` alongside the other services (a side-effect require, like `SpectateService`, that activates its death tracking).
- Skeleton only: no behavior changes, no new remotes, no UI.

### 2026-07-26 - `[major]` Phase 1: the Banishment is real (braziers, the Convergence, Desecrate, Rage)

- New `ServerScriptService/Services/RitualService.lua` - the whole Phase 1 arc (docs/DESIGN.md sections 3, 5 and 9) in one service. Cycle-safe: nothing requires it except `Bootstrap` and `SabotageService`; it requires `MatchService`/`TaskManager`/`KillSystem`/`LightsSystem` and reaches back only through hooks and Bootstrap injection. Tuning block: `CHANNEL_TIME = 12`, `CHANNEL_DECAY_ON_DEATH = 0.33`, `RAGE_STEPS` at 3/5/7 braziers (cooldown multipliers 0.9/0.8/0.7), `STATUS_TICK = 0.5`.
- **Braziers** - parts tagged `Brazier` carrying a numeric `BrazierIndex`; the registry is sorted by index and rebuilt each match start, warning on missing or duplicate indices. Lighting toggles visual children BY NAME: a `Flame` part (Transparency 0 lit / 1 unlit), its `PointLight` and a `Fire` if present. The lit set is an ordered stack so Desecrate always takes the most recent.
- **Threshold** - computed once per match as `min(#braziers, total tasks assigned across all crew)`, so small playtests can always arm the ritual and the map's brazier count is the ceiling, not the requirement. Warns loudly under 3. Computed via `task.defer` because `OnMatchStart` callbacks run *before* `StartMatch` assigns roles and tasks.
- **Lighting** - each `TaskManager.OnTaskCompleted` lights the lowest-index unlit brazier. Completions past the threshold while already armed do nothing; whether surplus should bank is left as an open design question, not treated as a bug.
- **The Convergence** - on a Heartbeat while armed, channelers are living crew whose HumanoidRootPart is inside the part tagged `ConvergenceZone` (centre from Position, radius from the widest horizontal axis of Size; warns if the tag is missing). `channelRequired = max(2, ceil(livingCrew / 2))`; progress accrues at `dt / CHANNEL_TIME` while the requirement is met and otherwise HOLDS - stepping out costs time, not progress. At 1.0 the ritual completes and calls `MatchService.EvaluateWinCondition("Ritual")`.
- **Interrupt** - via `RoleManager.OnAliveChanged`: a channeler dying decays progress by 0.33, floored at 0, never zeroed, and broadcasts immediately.
- **Desecrate** - `RitualService.Extinguish()` snuffs the top of the lit stack and returns false when nothing is lit. Dropping back below the threshold while armed disarms the ritual and zeroes the channel - the Vessel's climax comeback tool by design.
- **The Entity's Rage** - every lit-count change recomputes the step and pushes it to three consumers: `KillSystem.SetCooldownMultiplier`, `SabotageService.SetCooldownMultiplier` (through the new `RitualService.OnRageChanged` hook, so RitualService never requires SabotageService back), and `LightsSystem.SetRageDim`. All three reset to neutral at match start.
- New `RitualStatus` remote (`{ lit, total, threshold, armed, channelProgress, channelRequired, channelersPresent, complete }`), broadcast on every state change and every 0.5s while armed, plus a per-player snapshot on join and again 3s later (the established client-listener timing pattern).
- **`MatchService`: tasks no longer win matches.** The Phase 1 seam is swapped - the default task-completion provider and the `TaskCompleted` trigger registration are deleted outright, not disabled, and the crew objective is now `RitualService.IsComplete`. Because `RitualService` requires `MatchService`, the provider is injected by `Bootstrap` (`SetCrewObjectiveProvider` was already public for exactly this) rather than required back. `includeParity` is now unconditionally true: its only exclusion was `TaskCompleted`, which is gone, and `"Ritual"` is no exception - the Convergence is the crew objective itself and wins through the ordinary `CheckWinCondition` flow.
- **`SabotageService`: Desecrate as a third, INSTANT sabotage type** - `{ instant = true, critical = false }` with no stations. Instant types never set `active` and never open a fix window; they share the match/meeting/alive/Vessel gates and then branch off. Desecrate runs on its own 45s `desecrateReadyAt`, independent of the shared sabotage cooldown, so it may fire mid-Lights or mid-Manifestation - it is a jab, not a phase. Nothing lit rejects with `NothingToDesecrate` and does *not* consume the cooldown; success broadcasts `SabotageStatus { type = "Desecrate", instant = true }` as the global "the Entity stirs" omen. New `SetCooldownMultiplier` scales both the shared and Desecrate cooldowns when a ready time is set; match start resets the multiplier to 1 and applies the opening grace period to `desecrateReadyAt` too.
- **`KillSystem`** gained `SetCooldownMultiplier`; future kill cooldowns (and the `KillFeedback` chip, which counts the same value) scale by it, reset to 1 at match start. Nothing else changed.
- **`LightsSystem`** gained `SetRageDim(step)`: every `RoomLamp` PointLight's Brightness becomes `base * (1 - 0.15 * step)`, with the originals cache extended to capture base brightness. Independent of lights-out - snuffed lamps are unaffected, and restoring them restores to the *raged* brightness, not full, so fixing the fuse box can't undo the Entity's Rage.
- Known follow-up, not in this scope: the client `SabotagePanel` still lists only Lights and Boiler, so Desecrate has no button yet, and no client reads `RitualStatus` - the brazier/Convergence HUD is the next pass.

### 2026-07-26 - `[fix]` Ritual threshold: lazy memoization instead of a deferred read

- Root cause: `MatchService.StartMatch` **yields** at its `LoadCharacter` loop before it reaches `TaskManager.AssignTasks`, so the `task.defer` scheduled from `RitualService`'s `OnMatchStart` callback could execute mid-yield and read the *previous* match's task count - or zero on a fresh server, which computed a 0 threshold and armed the Convergence instantly.
- The deferred `computeThreshold` is gone. `OnMatchStart` now only sets `threshold = nil` alongside the existing resets.
- New `getThreshold()` computes on first READ and memoizes: while `TaskManager.GetTotalCount()` is 0 (the pre-assignment window) it returns 0 and memoizes nothing, so the first read after assignment is the one that sticks. The `min(#braziers, totalTasks)` formula and the loud warning under 3 are unchanged.
- Every consumer routes through `getThreshold()`: arming, the disarm check in `Extinguish`, the `RitualStatus` payload and the `PlayerAdded` snapshots. Arming additionally requires `getThreshold() > 0`, so a pre-assignment 0 can never arm; a 0 in a snapshot is harmless because the UI already hides at `lit == 0`.
- `RitualService.lua` only; no other file touched.

### 2026-07-26 - `[major]` Ritual UI + Desecrate on the panel; Flashlight retired pending rework

- New `StarterPlayer/StarterPlayerScripts/RitualUI.client.lua` - the Banishment on screen, UIStyle throughout. A top-center pill at `(0.5, 0, 0, 94)`, third in the stack under RoundStatus and SabotageBanner, driven entirely by `RitualStatus` (no ticking, no polling). Hidden at `lit == 0` and unarmed; `Braziers X/THRESHOLD` with the count Accent-colored via RichText once anything is lit; on arming the pill takes an Accent stroke, reads `THE RITUAL IS ARMED - hold the circle`, and grows a 4px channel bar (Row track, Positive fill) plus a `channelers P/R` line. The bar is set directly and never tweened, so a death-interrupt's 33% decay reads as a drop.
- `THE BANISHMENT` shows for 3s on completion and then steps aside - `EndScreenUI` still owns the end screen. The pill also clears on `MatchEnded`, which is what keeps a stale brazier count off the end screen and out of the next round when a match ends by parity, a verdict or Manifestation (the server sends no ritual reset broadcast).
- The Desecrate omen rides in on `SabotageStatus`, not `RitualStatus`: a `{ type = "Desecrate" }` payload flashes the pill Negative with `The Entity stirs...` for 2s (session-token guarded, forced visible even when the snuffed brazier was the last one) and then restores the real state. Everyone sees it - that is the omen.
- `SabotagePanel` gained a third row, **Desecrate the Circle**, firing `TriggerSabotage("Desecrate")`. It mirrors its own independent 45s cooldown, armed on the omen broadcast, plus the 20s match-start delay - so the row can read Ready while Lights or the Boiler is running. Panel grew 170 -> 220px. `NothingToDesecrate` maps to "No flames to snuff", and a rejected press consumes nothing. Instant payloads now short-circuit the status handler: without that they would have been read as "the running sabotage ended", wrongly arming the shared cooldown every time Desecrate fired mid-Lights. Lights/Boiler rows are otherwise untouched.
- **Flashlight retired pending rework** (docs/DESIGN.md section 7's Wick redesign). `CharmDefs`' Flashlight entry gains `enabled = false` behind a loud comment; absent means enabled, so no other Charm changes. Ownership data - tier and banked duplicates - is preserved everywhere and must never be deleted.
- `GachaService` excludes disabled Charms from the roll pool; remaining weights renormalize against the smaller total with nothing revalued. Pity is unchanged. The catalog still carries every Charm, now with an `enabled` flag so clients can dim them.
- `PowerupService.GetOdds` also excludes disabled Charms from both the weight total and the returned list. This was not optional: that function exists so displayed odds and the actual roll can never drift apart, and leaving a non-rollable Charm in the total would have made every other Common's percentage wrong.
- `PowerupService.TryUse` rejects disabled ids with `Retired` as a backstop. The Flashlight effect handler and its lights-restored cancellation stay in place, both marked DORMANT dead code kept as the reference the Wick rework starts from.
- `LoadoutService` rejects disabled ids with `Retired` (checked before the ownership test, so an owner is told the truth rather than "NotOwned"), and the match-start promotion silently drops disabled ids from the active clone. No mid-match surgery and no rewriting saved data - the pending entry survives, the Charm just stops arriving, and un-retiring needs no migration.
- `HubUI` renders disabled Charms dimmed with a TextDim "Being reworked" tag in both the catalog and the loadout picker, with tier/duplicate progress still visible (that data is preserved, and the row saying so is how the player knows). No Upgrade button, not selectable, and a retired id is stripped from the pending selection on render - otherwise a Charm retired after a save would be stuck selected on an inert row and every Save would fail. Retired Charms are also excluded from the gacha spin pool, so the reveal only ever flickers through outcomes that were genuinely possible.
- `PowerupFX`'s flashlight fog-override path is comment-marked dormant; it now always falls through to the crew value. `PowerupHUD` needed no change - a retired Charm never occupies a slot.
- Docs updated in the same pass: DESIGN.md marks the Wick row RETIRED and adds the rework to section 12; ROADMAP.md adds it to the deferred list.

### 2026-07-26 - `[fix]` RitualService broadcasts its match-start reset

- `RitualService`'s `OnMatchStart` handler now calls the existing `broadcast()` as its last step, after every reset is complete (braziers unlit, stack cleared, `armed`/`complete` false, channel zeroed, rage neutral, threshold cleared), so all clients receive the zeroed `RitualStatus` immediately instead of waiting for the match's first task completion.
- Restores the broadcast-on-every-state-change contract the service documents: the status stream is authoritative, and no client or future consumer has to infer a reset from `MatchEnded`.
- `RitualUI`'s `MatchEnded` hide stays in place as redundancy, not as the mechanism.
- One line plus its comment; `RitualService.lua` only, no other file touched.

### 2026-07-26 - `[major]` UNTESTED - Phase 2a: the Charm grammar's machinery, proven on Quickening

- **`CharmDefs` is now the full grammar data.** Display names re-themed with ids untouched: Speed Boost -> **Quickening**, Invisibility -> **Shroud**, Shapeshifter -> **Guise**, Seer -> **Augur**, Flashlight -> **Wick (reworking)**. New `CharmDefs.LOADOUT_WEIGHT_CAP = 4` (declared only - `LoadoutService` starts enforcing it next prompt).
- Every enabled Charm now carries both `faces` and an `omen`. For each Charm, the face that already shipped points at the *same* per-tier table as its `tiers` - wired in four explicit assignments at the bottom of the file because a Lua literal cannot reference itself, so tuning `tiers` moves both and they can never drift. The new faces carry initial tuning only: Quickening/Vessel `{ mult 1.6, duration 1.2/1.5/1.8, lungeRange 20 }`, Shroud/Crew `duration 4/6/8`, Guise/Crew `decoyDuration 8/10/12`, Augur/Vessel `{ markDuration 6/8/10, cooldown 30 }`.
- Omens declared per Charm as `{ type, intensity }` - Draft / ColdTrail / MirrorChime / Whisper - with **intensity always the Charm's own rarity**. That is the Omen law made structural: a rarer Charm cannot be quieter than a common one, because loudness and rarity are the same field.
- **New `OmenService`** (`Emit`, `EmitSustained`), the single emitter of world-visible tells. `RADIUS = { Common 30, Rare 40, Epic 50 }` is the master loudness dial. It requires only `Remotes` and nothing requires it back, so anything may require it - deliberately a leaf that is only ever called into.
- `Emit` positions the omen at an override or the source's `HumanoidRootPart` (aborting silently if neither exists - a missing character must never error out an activation that already succeeded) and fires the new `OmenEvent` to every player inside the radius, **the source included**. You perceive your own omens by design: that is how a player learns what their Charm costs them.
- `EmitSustained` pulses `Emit` on an interval, identity-token guarded, self-cancelling when the source loses their character/root/Humanoid or dies, and returns a canceller. **Unused today** - built now so the next batch only consumes it.
- New `OmenEvent` remote (Server -> Client): `{ omenType, position, intensity, sourceUserId }`.
- **`PowerupService` resolves faces.** The flat `effectHandlers` table became `charmHandlers[id] = { Crew = fn, Vessel = fn }`, selected in `TryUse` *after* every existing gate passes - match state, meeting, retired, equipped, owned, already-active, per-match uses and cooldown are all unchanged and all role-blind, so whether a use is allowed still never depends on your role. The Vessel role gets the Vessel face; every other case, crew and defensively roleless, gets Crew. Handlers now take `(player, stats, tier)`; both faces of a Charm register under the same effect-registry key, so cooldowns, the AlreadyActive gate, meeting/death cancellation and the kill-break rule keep working without knowing which face ran.
- **Quickening got its real faces.** Crew is the existing speed effect verbatim, read through `faces.Crew.tiers`. Vessel applies `mult` for the short Vessel duration via the same registry/token pattern, and on start finds the nearest other living player within `lungeRange` and sets the user's root `CFrame` to face them once - **orientation only, no impulse**: a physics kick on a client-owned character replicates as a shove, fights the Humanoid's own movement, and doubles as a griefing tool. No target in range means the burst still fires, just without the orient. Both faces emit the `Draft` omen at activation.
- **Shroud, Guise and Augur alias their existing single implementation to BOTH faces**, each marked `TEMPORARY ALIAS - real face lands in the Phase 2 batch`. Their behavior is byte-identical to before this change and they emit no omen yet; the aliases exist so the machinery could ship without a balance change riding along.
- **New `OmenFX.client.lua`**, the single renderer for `OmenEvent`: a handler table keyed by `omenType`, unknown types no-op by design (an omen may ship server-side before its renderer). It never re-checks distance or role - the server already decided who hears an omen. Only **Draft** is implemented: 6-10 grey-brown motes kicked outward off the floor plus one faint Neon wind-streak, all gone inside ~0.45s, scaled subtly by intensity. `ColdTrail`/`MirrorChime`/`Whisper` are stubbed with comments naming the batch prompt. No sounds in this pass.
- Transient parts live under one `OmenFX` folder swept on `MatchEnded` and `CharacterAdded`, with a generation token so an in-flight effect finds itself stale instead of resurrecting into the next life or match. Deliberately **not** gated on `ClientSettings.GetReduceEffects()` - an Omen is not flair, it is the counterplay guarantee, and a player who could mute omens would be buying silence for everyone else's Charms.
- Flashlight is untouched beyond its display name; its dormant handler is now registered under both faces purely to keep the shape uniform.
- Next up: the real Shroud/Guise/Augur faces plus their omens, and `LoadoutService` enforcing `LOADOUT_WEIGHT_CAP`.

### 2026-07-26 - `[major]` UNTESTED - Phase 2b: Shroud, Guise and Augur get real faces; the weight budget bites

- **Every temporary alias is gone.** All four enabled Charms now have two real faces; each Charm's pre-existing behavior survives as exactly one of them, unchanged. No gate, no face-resolution code and no effect-registry plumbing was restructured - this batch only consumes what Phase 2a built.
- **Shroud (Invisibility)** - one concealment implementation, two durations: Vessel keeps the tiers (and the stalking approach) it always had, Crew gets the shorter veil-step at 4/6/8s. The **kill-break rule needed no per-face branch**: `KillSystem.OnKillPerformed` cancels the effect on the killer, and crew cannot kill, so the hook simply never fires for the Crew face. One rule, one place.
- Shroud's omen is a **trail, not a moment** - `OmenService.EmitSustained("ColdTrail", ..., 1.2)`. The returned canceller is stored **on the registry entry** and called from `entry.cancel`, so duration expiry, meeting start, death, match start and any manual `cancelEffect` all stop the trail. It cannot outlive the invisibility that pays for it.
- **Guise (Shapeshifter)** - Vessel keeps the disguise verbatim, every truth-preservation rule intact (`Player.Name` still never touched). Crew gets the **decoy afterimage**: `Archivable` flipped for one `:Clone()` and put straight back, every `BaseScript` stripped from the clone (Animate above all, or the afterimage would breathe and wander), every `BasePart` anchored, clone `Humanoid.DisplayDistanceType = None`, model named the user's current DisplayName, pivoted to the user and parented to `workspace`.
- The decoy **is not a Player**, so it is unkillable and unreportable *by construction* - `KillSystem` only resolves against Players and reports only find real corpses; nothing had to be taught about it. Its cost is readability: it never moves, so anyone who walks up knows what they are looking at. While it stands, the **user's own nametag is hidden too** and restored when it ends - both figures nameless is the entire alibi.
- Decoy lifetime is `decoyDuration` (8/10/12s) through the existing token pattern, registered under the same `Shapeshifter` key - which means the death, meeting-start and match-start cancels it needs were **already registered once at module level** and no per-activation connection is made anywhere. One decoy per player; the face defensively cancels first so reactivation replaces rather than accumulates.
- Guise's `MirrorChime` fires **at the decoy's position** for the Crew face and at the user for the Vessel face. The user may be standing outside their own omen's radius - that is the trade the Charm sells.
- **Augur (Seer)** - Crew keeps the soul read and its per-match uses. Vessel gets **mark prey**: same proximity rule as the read, then `PowerupEffect("AugurMark", "Start", { targetUserId, duration })` to the **user only**, with no server state held beyond the cooldown `TryUse` already commits. No valid target rejects with the existing `NoTargetNearby`.
- Both Augur faces emit `Whisper` - **reading souls is loud in both directions**, so a whisper tells the room someone is looking and never who or which way. Symmetry is the mechanic; it would be worth nothing if only one role paid.
- **Failures emit no omens, ever.** Every `emitCharmOmen` call sits after the last point its face can still return false - a rejected use is not power spent and must not cost information.
- **`LoadoutService` enforces the weight budget.** `SetLoadout` sums `loadoutWeight` across the submitted ids and rejects totals over `CharmDefs.LOADOUT_WEIGHT_CAP` with `OverWeight`. Double-Epic is now impossible by construction rather than by tuning Charms down; a Binding's +1 rides the same sum in Phase 4. Checked *after* every per-entry test, so a player always hears the most specific truth first (`Retired`, `NotOwned`) and only hears about the budget once the loadout is otherwise legal.
- Over-cap pendings saved before the cap existed are **grandfathered**: they keep promoting and keep playing until their owner's next save attempt, which is what fails. The match-start promotion is deliberately untouched - same no-retroactive-surgery principle as the retirement drop, and nobody gets silently disarmed at a match start they were not present for.
- **Known gaps, deliberately left, all commented at their site.** (1) Augur's Vessel face consumes a per-match *use*, because `usesPerMatch` is gated and spent by the shared role-blind gate before any face is chosen - the design intent is the opposite, and fixing it means making a shared gate face-aware, which this prompt ruled out. (2) `faces.Vessel.cooldown` (30) happens to equal the Charm's own cooldown (30), so the right number applies today by coincidence, not by wiring. (3) The decoy keeps the rig's collision, so it is a solid object in the room - a playtest question, not a code decision.
- **Not touched, and now needing a follow-up pass:** `OmenFX` still stubs `ColdTrail`/`MirrorChime`/`Whisper`, so three omens now broadcast with no renderer (they no-op harmlessly); `PowerupFX` has no `AugurMark` handler, so the mark lands but draws nothing; `HubUI` shows no weight readout, so an over-cap save is rejected without the UI ever having said why; and `CharmDefs`' header still describes the three faces as aliased. All four are outside this prompt's file list.

### 2026-07-26 - `[fix]` Face-aware resource gates: Augur's two currencies stop bleeding into each other

- Root cause: `TryUse` ran every gate before resolving the face, so the per-match-uses gate read `usesPerMatch` off the Charm's shared tier row and charged **both** faces for it. Augur's Vessel mark spent one of the Crew face's scarce reads. The per-face `cooldown` had the mirror-image problem: `faces.Vessel.cooldown` (30) was never read at all, and only produced the right number because the Charm's own cooldown happened to be 30 too.
- **`TryUse` is now two gate stages with the face resolution between them.** Stage 1 is the state gates - match state, meeting, retired, equipped, owned, already-active - unchanged, still role-blind, and still costing nothing, so whether a use is *allowed* never depends on your role. Stage 2 is the resource gates, read from the resolved face.
- Resolution moved between the stages rather than after them because it is a pure lookup - it reads a role and picks a table, touching no cooldown, no counter and no world state - so running it earlier cannot cost a player anything even when Stage 2 goes on to reject. Commented at the site.
- Cooldown resolves as `face.cooldown or def.cooldown`: every Charm keeps a pacing floor, and a face overrides it only when its rhythm genuinely differs. The value returned to the client HUD is now that same resolved number.
- Per-match uses resolve from the face **with no charm-level fallback** - a budget one face never spends is not a property of the Charm. Declared as a per-tier array on the face, matching every other per-face tuning value. Seeding, the `NoUsesLeft` rejection and the spend-on-success all run against it.
- **`CharmDefs`: Seer's `usesPerMatch` moved out of `tiers` and onto `faces.Crew` as `{ 1, 1, 2 }`** - same values, same tiers, now unreachable from the Vessel face. The tier rows keep `minAlive` only. The Vessel face carries `markDuration` and its own `cooldown` and declares no uses at all.
- Net effect: Augur/Crew is unchanged (scarce reads, `minAlive` gate, 30s cooldown); Augur/Vessel is cooldown-only rhythm and can never draw down the read budget. **No other Charm's behavior changes** - no other face declares a `cooldown` or `usesPerMatch`, so the fallbacks reproduce today's values exactly, and Seer was the only Charm ever using per-match uses.
- Both cooldown and use tables stay keyed by Charm id alone, not by face: a player's role cannot change inside a match, so only one face of a Charm is ever reachable per player per match and the two can never collide.
- `CharmDefs`' header comment fixed - it still described Shroud/Guise/Augur as aliased, which stopped being true last prompt - and now documents the per-face resource fields and the no-fallback rule for uses.
- Unchanged and still open from the previous entry: no client renders `AugurMark`, `OmenFX` still stubs three omen types, and `HubUI` has no weight readout.

### 2026-07-26 - `[major]` UNTESTED - Phase 2 closes: all four omens render, the mark is private, the hub speaks weight

- **`OmenFX`: the three stubs are filled**, so every omen a Charm can emit now has a renderer. Rough-but-evocative placeholders, sized to playtest the Omen law and to seed the Phase 6 art/audio pass, none of them final.
- The rule is now written into the file above the handler table: **OMENS NEVER GATE ON `ReduceEffects`**. An Omen is game information the *other* players are owed, not flair for the person watching; a setting that muted omens would let one player buy silence for everyone else's Charms. Sounds respect the master volume because that is how all audio works and a muted game must stay muted; visuals are unconditional.
- **ColdTrail** (Shroud): 3-5 tiny blue-white motes drifting slowly upward with a faint `PointLight` per pulse. Deliberately the weakest of the three and commented as such - it arrives ~once a second for the whole concealment, so it is meant to be read by *watching* a corridor, not by being slapped in the face. What makes it findable is that it repeats.
- **MirrorChime** (Guise): a ring of 4 thin Neon shards flung outward and tumbling as they fade over 0.5s, plus a short high ping (`electronicpingshort`, speed 2.2, volume 0.28). The loudest of the three on purpose - a false image entering the world is the most consequential thing a Charm does to another player's read of the room.
- **Whisper** (Augur): a barely-visible dark ripple - a flattened Cylinder expanding once from transparency 0.86 - plus the same asset at half speed for a low breathy tone (volume 0.1). Almost entirely an audio omen by design: it should say *someone nearby is looking* and pointedly not who or which way.
- Sounds are engine-bundled only (no marketplace/moderation dependency, the `EchoCode` precedent), all at or under 0.35 volume, all routed through `ClientSettings.ApplyVolume`, and all **positional** - each rides an invisible anchored part at the omen's position inside the existing swept folder, because an omen you can hear but not locate is noise, not information. Cleanup is `Debris` plus the folder sweep.
- **`PowerupFX`: the Augur mark renders**, on `PowerupEffect("AugurMark", "Start", { targetUserId, duration })` - a `Highlight` on the target's character with `FillTransparency` 0.6, sickly-gold outline and `AlwaysOnTop` depth for the through-walls read.
- **The server never creates that Highlight, and the comment says why:** Highlights replicate, so a server-made one would glow on *every* client - the marked player would see themselves lit up and so would every bystander, turning a hunt into a warning. The server sends only an instruction to the marker; the mark exists on exactly one screen by construction.
- Mark cleanup is token-guarded and covers all five paths: the duration expiring, the target dying, the target leaving or losing their character, meeting start, and `MatchEnded`; an `"End"` payload clears it early, and a second mark always replaces the first. `PowerupFX` now listens to `MeetingStarted`/`MatchEnded` for this - the mark is client-only state with no server counterpart, so these are its only cleanup.
- **`HubUI`: the weight budget is visible.** The file now requires `CharmDefs` directly (pure data, client-loadable) rather than routing weights through the catalog, so the number on screen and the number the server rejects against come from the same table and cannot drift.
- Each loadout row gained a small rarity-tinted **weight chip** (`W1`/`W2`/`W3`) on its right edge - weight *is* rarity, so the two are shown as one fact - stepping left of the "Being reworked" tag on retired rows, and dimming with them.
- A running **`Weight X/4`** line sits beside Save, tinted Negative when the selection exceeds the cap. Selecting a Charm that would breach the cap is blocked client-side with a named notice ("<Charm> is too heavy for what's picked (cap 4)"), so the budget is a rule you learn where you bump into it rather than a wall you hit at Save. **The server stays the authority** - `LoadoutService` re-sums and rejects regardless, and the comment says the client check exists only to explain, never to enforce.
- `OverWeight` maps to "Too heavy - lighten the loadout" on the save path, with the raw-reason fallback unchanged.
- Confirmed: `HubUI` holds **no cached pre-retheme names** - every Charm name it draws (loadout rows, active-loadout line, catalog rows, reveal card, upgrade status) comes from the catalog's `displayName`.
- **`PowerupHUD`**: `OverWeight` added to the failure-reason map defensively - it is a `LoadoutResult` reason that `TryUse` cannot currently produce, mapped so a future use-time weight check reads as English on day one. Slot labels confirmed to render catalog `displayName`s.
- **Found but deliberately NOT changed** (outside the listed scope): `PowerupHUD`'s Seer reveal toast is still hard-coded `"Seer: <name> is <role>"` - a pre-retheme display name that should read "Augur", and a role value that renders the internal `Crewmate`/`Impostor` ids rather than Crew/Vessel. Worth folding into the display-layer re-theme pass.

### 2026-07-26 - `[fix]` The reveal toast reads Augur, and role ids stop reaching the screen

- Closes the finding flagged in the previous entry. `PowerupHUD`'s reveal toast was the last hard-coded pre-retheme Charm name, and it rendered the raw role id - so a successful read printed `Seer: <name> is Impostor`.
- New `GameConstants.RoleDisplayNames` maps the internal role ids to their labels (`Crewmate` -> "Crew", `Impostor` -> "Vessel"). The rule is commented at the map: the display layer reads this, never the ids. An id being a readable English word is exactly what let one leak onto a screen in the first place - it is a stable comparison and persistence value, not a label.
- The map is knowingly partial today; only the surfaces re-themed so far route through it. The identity pass's fuller terminology sweep (end screen, meeting wording, the remaining labels) consumes this same map rather than adding its own.
- The toast now builds both halves from data: the Charm name from `CharmDefs.Charms.Seer.displayName` (so it reads "Augur" and follows any future rename by itself) and the role through `RoleDisplayNames`, with the raw id kept as a fallback so an unmapped role degrades to something truthful rather than to blank.
- `PowerupHUD` gained requires for `CharmDefs` and `GameConstants` - both pure data with no requires of their own, and the toast is their only reader in that file. Nothing else in it changed.

### 2026-07-26 - `LIVE_LOADOUT` debug flag: mid-match Charm cycling for verify sessions

- New `DebugFlags.LIVE_LOADOUT` (ships false, loud warn on server start like `ASSIGN_ALL_TASKS`). With it on, a loadout save promotes straight onto the ACTIVE set instead of staging for the next match, so a verify session can cycle through Charms without restarting a round between each.
- Flagged in the comment as the one toggle that is not merely untidy if it ships true: the pending/active split is **load-bearing for the dual-face law** (docs/DESIGN.md section 7). Builds lock before roles are assigned, which is the whole reason every Charm must be worth a slot to either role - a player who can re-pick after learning their role brings a role-optimal build every match and the dual-face design stops meaning anything.
- `LoadoutService` requires `DebugFlags` (a leaf - booleans, no requires of its own, no cycle).
- The promotion sits at the very end of `SetLoadout`, after every existing validation has passed and after pending is staged as normal, so the flag can only ADD an effect. Ownership, slot count, duplicates, retirement and the weight budget are all still enforced - a debug flag that masked a validation bug would make the thing it exists to test untrustworthy.
- It reuses the existing `LoadoutApplied` remote to the saving player only, so `PowerupHUD` re-renders its slots through the listener it already has. **No client changes.**
- A Charm mid-effect when swapped out is deliberately **not** cancelled: its effect ends through its own paths (duration, meeting, death, match start) exactly as it would have, and only a future `TryUse` reads the new active set. Cancelling here would mean reaching into `PowerupService`'s effect registry from this service - a require cycle, and a second owner of effect lifetime.
- With the flag false the block is skipped entirely and `SetLoadout` behaves byte-identically to before.

### 2026-07-26 - `[major]` UNTESTED - Debug rehaul: runtime, mod-gated flags and actions

- **`DebugFlags` stops being a file you edit.** Module name, every field name and the false defaults are unchanged, so every existing consumer works untouched - but toggling is now runtime and session-only: `DebugService` mutates the fields live and a server restart resets all of them. Nothing can be committed on by accident, because turning something on is no longer a code change.
- Three new flags, all false: **`GOD_MODE`** (mods can't be killed), **`FAST_INTERMISSION`** (3s countdown), **`KILL_NO_COOLDOWN`**.
- The loud when-enabled warnings left module scope entirely - a flag that is false at boot has nothing to warn about. They now fire on `DebugService`'s toggle path, on the **off -> on edge only**, naming the flag, the mod who flipped it and their UserId.
- **Consumer audit - every read site now happens at decision time.** Three required fixing, all require-time reads that runtime toggling would have frozen at boot: `Bootstrap`'s `ALL_IMPOSTORS` startup `warn`, `RitualService`'s `ALL_IMPOSTORS` informational `print`, and `SabotageService`'s equivalent. All three were deleted (with `RitualService` and `SabotageService` losing their now-unused `DebugFlags` require), since with false defaults they could never have fired anyway. Every other site - `RoleManager.AssignRoles`, `TaskManager.AssignTasks`, `KillSystem.AttemptKill`, `LoadoutService.SetLoadout`, `MatchService.EvaluateWinCondition`, and both `Bootstrap` handlers - already read inside the function that acts on the value and was left alone.
- **New `DebugService`**, sitting at the top of the dependency graph: it requires the services it drives and nothing requires it back.
- `MOD_USER_IDS = {}` at the top of the file with instructions to add the owner's UserId; `RunService:IsStudio()` grants mod to everyone, which is a playtesting convenience only - in a live server the list is the sole way in. It warns at boot if the list is empty outside Studio, since that means nobody can open the menu.
- **`IsMod` is re-checked server-side inside every remote handler**, and rejections are silent - a non-mod probing the remotes learns nothing, not even whether a flag name is real. The comment states the model plainly: a client hiding a menu is cosmetics, this check is the lock. `Bootstrap` deliberately performs no check of its own so there is exactly one place it lives.
- New remotes: `GetDebugState` (RemoteFunction; returns `nil` to non-mods, which doubles as how a client learns not to draw the menu), `SetDebugFlag`, `RunDebugAction`, and `DebugStateChanged` - broadcast to **current mods only** so several open menus stay in sync with each other.
- A flag registry (`name -> { label, description }`) covers every field and is what the menu renders; a flag absent from it cannot be toggled through the menu at all, which makes it the single place to register a new one.
- **Nine actions**, each `pcall`-wrapped with a warn on failure: `ForceStart`, `EndCrewWin`, `EndVesselWin`, `LightBraziers`, `ExtinguishBraziers`, `GiveCurrency` (+1000), `MaxCharms`, `ResetCooldowns`, `KillMe`.
- **Debug entry points on the owning services**, each marked DEBUG ONLY and inert when unused - `DebugService` never reaches into anyone's internals:
- `MatchService.DebugForceStart()` sets a **request the round loop consumes on its next poll** rather than calling `StartMatch` directly, so the loop stays the single owner of round progression and can never be raced into starting two matches. It also releases the wait-for-players loop, which is what lets a solo verify session start a round at all, and is cleared at `StartMatch` so it can only ever skip one intermission. The countdown length reads `FAST_INTERMISSION` at the countdown's start.
- `RitualService.DebugLightAll()` / `DebugExtinguishAll()` go through the same `setLit`/`applyRage`/`broadcast` paths a real task completion or Desecrate uses, so Rage steps, lamp dimming and the client HUD all update exactly as they would in play.
- `KillSystem`: `GOD_MODE` rejects a mod target from the same place in the sequence an invalid target is rejected, so the killer keeps their cooldown and nothing downstream can tell the difference; `KILL_NO_COOLDOWN` skips only the *arming*; `DebugKill` runs the real death conversion (same `SetAlive`, ragdoll, `OnAliveChanged` fan-out and win check) but deliberately does **not** fire `OnKillPerformed` - nobody performed it, so nothing may react as though someone did.
- `KillSystem.SetModPredicate` is the one reverse channel: `DebugService` injects `IsMod` into `KillSystem` at its own require time, so the dependency still points one way and no cycle is created.
- `PowerupService.DebugResetCooldowns` clears cooldowns **and** per-match uses (a verify pass firing Augur repeatedly needs both), leaving active effects to end through their own paths.
- **Note a deliberate divergence:** `PowerupOwnershipService.DebugMaxAll` **persists** (`syncEntry` -> `MarkDirty`), unlike the existing session-only `DebugGrantMax` beside it. A mod granting themselves a collection on their own account wants it to survive a rejoin - but it does make this the one debug path that writes to the DataStore, which is a reason to keep `MOD_USER_IDS` tight. Commented at the site.
- Retired Charms are skipped by `DebugMaxAll` - granting something that can never be equipped would only make the inventory lie.
- **No client menu exists yet.** The server surface is complete and callable, but nothing draws it; the mod menu UI is the next pass.

### 2026-07-26 - `[major]` UNTESTED - The debug menu lands in Settings, mods only

- `SettingsUI` gains a fifth section, **DEBUG**, built last so it sits at the bottom of the existing single scroll surface below everything a normal player opened Settings for. `SettingsUI.client.lua` is the only file touched.
- The state is fetched on **every open**, not once: both mod status and the flag values can change during a session, and re-asking is cheaper than keeping a stale menu honest. `setOpen` gained one hook call for this and is otherwise unchanged.
- **A `nil` answer means nothing is built** - no header, no placeholder, no hidden frame. A non-mod's window is pixel-identical to what it was before this existed, and there is no debug section on their client to find. The fetch is token-guarded so a fast close-and-reopen can't have two in-flight builds, and `pcall`-wrapped so a failed call can never break opening Settings.
- The section label is **Negative-tinted**, unlike every other section's Accent, so it never reads as one more player feature.
- **Toggle rows** are styled exactly like Reduce Screen Effects: label left in the body font, a 60x28 ON/OFF `MakeButton` right, Positive when on. Each carries a one-line TextDim description straight from the server's flag registry, so the menu's copy has a single source.
- Clicking paints optimistically and fires `SetDebugFlag`; the `DebugStateChanged` broadcast is what makes it true, what corrects the row if the server refused, and what keeps a second mod's open menu in step with the first.
- **Action rows** are full-width buttons labelled from the server's action registry, firing `RunDebugAction` and flashing Positive for 0.25s. Deliberately no result reporting: what an action did is observed in the world - a match starts, braziers light, you drop dead - not in this menu.
- Rows are ordered alphabetically rather than by `pairs()` order, so the menu doesn't reshuffle itself between opens.
- Footer reads "Session only - resets on server restart."
- Every instance the section owns is tracked so a rebuild removes all of it and touches nothing else in the window.
- **This is presentation only, and says so at the top of the section.** `DebugService` re-validates mod status on every remote it receives, so nothing on the client is load-bearing for access - a non-mod firing these remotes by hand is rejected server-side regardless of what their menu does or doesn't draw.

### 2026-07-26 - `[major]` UNTESTED - One `NO_COOLDOWNS` toggle replaces the Reset Cooldowns action

- **`ResetCooldowns` is gone entirely** - the action, its registry entry, and `PowerupService.DebugResetCooldowns`. It cleared server state at a moment in time, which is the wrong shape for verification: the next use re-armed the cooldown and the client HUD kept counting down the value it had already been told, so a mod pressing it saw a slot that still looked and behaved as though it were on cooldown.
- **`KILL_NO_COOLDOWN` is merged into a new `NO_COOLDOWNS` flag** covering all four cooldowns at once: Charm cooldowns, the kill cooldown, the shared sabotage cooldown, and Desecrate's independent timer.
- **A state toggle, not a one-shot** - the flag is read *at decision time inside every gate*, per the rule the debug rehaul rests on, so it applies for exactly as long as it is on and needs no cleanup when it goes off.
- **Gates pass; arming is untouched.** Every cooldown is still written normally - a gate that no longer consults the value makes the write harmless, and real pacing resumes the instant the flag is switched off, with no state to unwind.
- **The HUD is told 0** for Charms while the flag is on (`TryUse`'s third return, which exists only for the countdown chip), so `PowerupHUD` draws no overlay instead of greying out a slot that is in fact usable.
- **Per-match uses are deliberately NOT bypassed.** Augur's Crew read still spends from `usesPerMatch` - scarcity is one of the things a verify pass needs to test, and the old action wiped that budget along with the cooldowns.
- **No other sabotage gate is bypassed** - match state, meeting, alive, role and the one-sabotage-at-a-time `AlreadyActive` rule all still apply. Desecrate's `NothingToDesecrate` check likewise still rejects a press with no brazier lit.
- **Side effect, accepted:** the sabotage grace period at match start is stored in the same two ready-times as the cooldowns, so the flag lifts the opening delay too. That is the intended reading of "the cooldown gate passes."
- `SettingsUI` needed **no changes** - the client menu builds itself from `DebugService`'s flag and action registries, so the removed action and the renamed flag propagate on the next open.
- `PowerupService` and `SabotageService` now require `DebugFlags` (a plain table that requires nothing, so no cycle); `DebugService` no longer requires `PowerupService`.

### 2026-07-26 - UNTESTED - `KillSystem` cleanup after the `NO_COOLDOWNS` merge

- **`KillSystem.DebugResetCooldown` deleted** - orphaned when the `ResetCooldowns` debug action was removed. Nothing in `src/` called it; the only surviving mentions are historical entries in this file.
- **Stale comment corrected** - the DEBUG ONLY block above `DebugKill` no longer cites a `"ResetCooldowns"` action that does not exist, and now points at the `NO_COOLDOWNS` toggle that replaced it.
- **Killer's cooldown chip reaches parity with the Charm HUD** - `KillFeedback` reports `0` while `NO_COOLDOWNS` is on, read at decision time, so the chip shows no countdown against a kill the killer can already repeat.
- **Display value only.** The real cooldown is still armed on every kill exactly as before, so switching the flag off mid-session produces an honest countdown from the very next kill with no stale client state to correct.
- `KillSystem.lua` is the only file touched.

## Conventions

- **Tightly scoped prompts.** Each prompt lists exactly which files change and what changes in them; nothing else is refactored, renamed or reformatted, and files outside the list aren't touched.
- **Accepted behavior is explicit.** Prompts name the rough edges that are deliberate ("do NOT fix any of it") so placeholders and known debt don't get silently reworked.
- **Debug flags ship false.** Every toggle lives in `DebugFlags.lua`, nowhere else, and must be false to ship; some print a loud warning when enabled.
- **Map data lives in the place file.** Tagged parts, `TaskType`/`SabotageType`/`FixId` attributes and the test map are saved in Studio, not in this repo - keep the place published alongside the code.
- **Cycle-free services.** Cross-service reactions flow through registered hooks (`OnMatchStart`, `OnMeetingStart`, `OnLightsChanged`, `OnAliveChanged`, `OnSabotageChanged`) rather than mutual requires; the composition root (`Bootstrap`) wires anything that would otherwise cycle.
- **Commits are tagged.** `[major]` for feature arcs (with detailed multi-paragraph bodies), `[fix]` for corrections; commit bodies carry the reasoning and a "next up" list.
- **Placeholders are labeled.** Stubs say what replaces them and when.

## Unreleased / in progress

The working tree has substantial uncommitted work on top of `0a8ec82` (the last `[major]`), spanning several prompts:

- **Sabotage system** - new `SabotageService` (triggers, shared cooldown, the critical Boiler countdown that force-ends the match, fix sessions) and `SabotageStationHandler`; `RoleAssigned`/`SabotageStatus`/`TriggerSabotage` remotes; `MatchService.ForceEnd`; emergency-button and body-report gates; client `SabotagePanel` (C) and `SabotageBanner`; `FixSwitches` (Lights Out puzzle) and `FixValve` fix minigames, both click-only because impostors may legally open them and F is the kill key.
- **Darkness pass** - `LightsSystem` now kills tagged `RoomLamp` parts server-side so the world visibly darkens for everyone, while `PowerupFX` owns the per-role fog/ambient asymmetry, a personal crew "candle" light, and a reworked Flashlight (a head glow everyone can see, paired with a tier-scaled fog range).
- **UI hub era** - new shared `UIStyle` module (colors, Montserrat font faces, text strokes, builders, drag/resize helpers), a persistent `BottomBar`, and a tabbed `HubUI` (Store/Inventory) that absorbed and replaced the standalone loadout and gacha panels; every remaining HUD element restyled against `UIStyle`.
- **Gacha reveal** - a client-side decelerating spin/landing animation over the unchanged instant server roll, with rarity-colored results, a pop/pulse landing, an Epic flourish, and skip-to-landing.
- **Window behavior** - draggable, edge/corner-resizable windows with hover cursors and edge highlights, replacing an earlier handle-frame approach whose invisible hit regions caused stray resizes.

Not yet playtested as a whole, and the roadmap file still describes the older powerup and task designs.
